/**
 * cm_operators.hpp — Skeleton class declarations
 * Rockstar Presents Table Tennis (Xbox 360, 2006)
 *
 * 27 classes — originally auto-generated by gen_headers_v3.py from
 *   rtti_vtable_map | vtable_layout_map | offset_cluster_map
 *   | debug_string_field_map | rtti_class_hierarchy
 *
 * Concrete layouts (cmPort, cmCondEntry, cmCond, cmLookup) were added
 * below once confirmed by assembly analysis. Skeleton entries retain
 * their vtable slot addresses for targeted follow-up lifts.
 */
#pragma once
#include <stdint.h>

// ── cmPort — 8-byte data-flow graph port ────────────────────────────────────
// Each port is a {pointer, type} pair used in CM condition/lookup nodes.
//   type==1 (DIRECT): pData points to raw data object
//   type==2 (NODE):   pData points to an upstream CM operator node
struct cmPort {
    uint32_t    m_pData;        // +0x00  pointer to data or upstream node
    int32_t     m_type;         // +0x04  port type (1=direct, 2=node)
};

// ── cmCondEntry — 16-byte condition/value port pair ─────────────────────────
struct cmCondEntry {
    cmPort      condPort;       // +0x00  condition port (evaluated as bool)
    cmPort      valuePort;      // +0x08  value port (selected when cond is true)
};

// ── cmActionCtor  [2 vtables — template/MI] ──────────────────────────
struct cmActionCtor {
    void**      vtable;           // +0x00
};

// ── cmAndReporter  [vtable @ 0x82056544] ──────────────────────────
struct cmAndReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273a60
};

// ── cmApproach2Ctor  [8 vtables — template/MI] ──────────────────────────
struct cmApproach2Ctor {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmApproach2Ctor();                  // [0] @ 0x8226c658
};

// ── cmAvgReporter  [vtable @ 0x8205643C] ──────────────────────────
struct cmAvgReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void ScalarDtor(int flags); // [1] @ 0x82273cb0
};

// ── cmBitwiseAndReporter  [vtable @ 0x82056574] ──────────────────────────
struct cmBitwiseAndReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273b10
};

// ── cmBitwiseOrReporter  [vtable @ 0x8205658C] ──────────────────────────
struct cmBitwiseOrReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273b58
};

// ── cmCond  [5 vtables — template/MI] ──────────────────────────
// Layout (N-condition variant):
//   +0x00  vtable
//   +0x04  m_outputType     (set by RegisterPorts / vfn_16)
//   +0x08  m_flags
//   +0x0C  m_entries[0]     (cmCondEntry: 16 bytes — condPort + valuePort)
//   ...    m_entries[N-1]
//   +0x0C + N*16  m_defaultPort (cmPort, 8 bytes)
//
// 5 vtable instantiations: 2-cond, 3-cond, 4-cond, 5-cond, 6-cond
struct cmCond {
    void**      vtable;           // +0x00
    uint32_t    m_outputType;     // +0x04
    uint32_t    m_flags;          // +0x08
    cmCondEntry m_entries[6];     // +0x0C  (max 6; actual count depends on vtable variant)
    // After entries[N-1]: default port

    // ── virtual methods ──
    virtual void ScalarDtor(int flags); // [1] @ 0x8226ceb0
    virtual void vfn_2();  // [2] @ 0x8226ce28
    virtual void vfn_3();  // [3] @ 0x8226cdb8
    virtual void vfn_4();  // [4] @ 0x8226cd48
    virtual void vfn_5();  // [5] @ 0x8226ccd8
    virtual void vfn_16();  // [16] @ 0x8226cc98
    virtual void vfn_18();  // [18] @ 0x82270c10
};

// ── cmFirstReporter  [vtable @ 0x82056514] ──────────────────────────
struct cmFirstReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273a40
};

// ── cmFurthestFromReporter  [vtable @ 0x82056604] ──────────────────────────
struct cmFurthestFromReporter {
    void**      vtable;           // +0x00
};

// ── cmLastReporter  [vtable @ 0x8205652C] ──────────────────────────
struct cmLastReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273a58
};

// ── cmLinearRemap  [5 vtables — template/MI] ──────────────────────────
struct cmLinearRemap {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_2();  // [2] @ 0x8226f5c8
    virtual void vfn_4();  // [4] @ 0x8226f4c8
};

// ── cmLookup  [5 vtables — template/MI] ──────────────────────────
// Layout (2-entry variant, 68 bytes total):
//   +0x00  vtable
//   +0x04  m_cachedResult
//   +0x08  m_flags
//   +0x0C  m_keyPort        (cmPort, 8 bytes)
//   +0x14  m_entries[0]     (cmCondEntry, 16 bytes: threshold + result ports)
//   +0x24  m_entries[1]     (cmCondEntry, 16 bytes)
//   +0x34  m_defaultPort    (cmPort, 8 bytes)
//   Total: 0x3C = 60 bytes (2-entry); 3-entry adds +16
struct cmLookup {
    void**      vtable;           // +0x00
    uint32_t    m_cachedResult;   // +0x04
    uint32_t    m_flags;          // +0x08
    cmPort      m_keyPort;        // +0x0C  key value port
    cmCondEntry m_entries[3];     // +0x14  lookup entries (2 or 3 used)
    cmPort      m_defaultPort;   // +0x44  default port (3-entry); 2-entry uses (cmPort*)&m_entries[2]

    // ── virtual methods ──
    virtual void ScalarDtor(int flags); // [1] @ 0x8226db18
    virtual void vfn_2();  // [2] @ 0x8226d9c0
    virtual void vfn_3();  // [3] @ 0x8226da78
    virtual void vfn_4();  // [4] @ 0x8226d920
    virtual void vfn_5();  // [5] @ 0x8226d880
    virtual void vfn_16();  // [16] @ 0x8226d840
    virtual void vfn_18();  // [18] @ 0x82270d10

    // ── non-virtual methods (lifted in cm_operators.cpp) ──
    void RegisterPorts();
    void GetDim(int* outResult);
    void GetFloat(float* outResult);
    void GetBool(bool* outResult);
    void GetVector(void* outVector);
    void GetDimValue(int* outResult);
    void CopyState(void* dest);
};

// ── cmMaxReporter  [vtable @ 0x820565D4] ──────────────────────────
struct cmMaxReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273fe8
};

// ── cmMinReporter  [vtable @ 0x820565BC] ──────────────────────────
struct cmMinReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273ec0
};

// ── cmMostReporter  [vtable @ 0x820565A4] ──────────────────────────
struct cmMostReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmMostReporter();                  // [0] @ 0x82273d98
    virtual void ScalarDtor(int flags); // [1] @ 0x82273dc8
    virtual void vfn_3();  // [3] @ 0x82273e10
};

// ── cmNaryOperator  [4 vtables — template/MI] ──────────────────────────
// Confirmed methods: Evaluate
struct cmNaryOperator {
    void**      vtable;           // +0x00

    // ── non-virtual methods (from debug strings) ──
    void Evaluate();
};

// ── cmNearestToReporter  [vtable @ 0x820565EC] ──────────────────────────
struct cmNearestToReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmNearestToReporter();                  // [0] @ 0x82274110
    virtual void vfn_3();  // [3] @ 0x82274138
};

// ── cmNoneReporter  [vtable @ 0x820564FC] ──────────────────────────
struct cmNoneReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void ScalarDtor(int flags); // [1] @ 0x82273a28
};

// ── cmOperatorCtor  [113 vtables — template/MI] ──────────────────────────
struct cmOperatorCtor {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmOperatorCtor();                  // [0] @ 0x82185a38
};

// ── cmOrReporter  [vtable @ 0x8205655C] ──────────────────────────
struct cmOrReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273ab8
};

// ── cmRefreshableCtor  [20 vtables — template/MI] ──────────────────────────
struct cmRefreshableCtor {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmRefreshableCtor();                  // [0] @ 0x82267ea0
};

// ── cmReporterCtor  [14 vtables — template/MI] ──────────────────────────
struct cmReporterCtor {
    void**      vtable;           // +0x00
};

// ── cmSmoothRemap  [5 vtables — template/MI] ──────────────────────────
struct cmSmoothRemap {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_2();  // [2] @ 0x822700f0
    virtual void vfn_4();  // [4] @ 0x8226ffe0
};

// ── cmSumReporter  [vtable @ 0x82056424] ──────────────────────────
struct cmSumReporter {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void vfn_3();  // [3] @ 0x82273c00
};

// ── cmSwitch  [5 vtables — template/MI] ──────────────────────────
struct cmSwitch {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual void ScalarDtor(int flags); // [1] @ 0x8226ea90
    virtual void vfn_2();  // [2] @ 0x8226e988
    virtual void vfn_3();  // [3] @ 0x8226ea18
    virtual void vfn_4();  // [4] @ 0x8226e910
    virtual void vfn_5();  // [5] @ 0x8226e898
};

// ── cmWorldApproach2Ctor  [8 vtables — template/MI] ──────────────────────────
struct cmWorldApproach2Ctor {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmWorldApproach2Ctor();                  // [0] @ 0x8226c068
};

// ── cmWorldRefreshableCtor  [18 vtables — template/MI] ──────────────────────────
struct cmWorldRefreshableCtor {
    void**      vtable;           // +0x00

    // ── virtual methods ──
    virtual ~cmWorldRefreshableCtor();                  // [0] @ 0x82267f40
};
